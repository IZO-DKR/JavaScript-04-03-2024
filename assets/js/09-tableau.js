/*------------------------TABLEAUX---------------------*/

/*
un tableau (array) est un type de donnée qui peut contenir plussieurs elements 
On peut créer un tableau en utilisant des crochets [] et en y insérants les elements séparés par des virgules
*/

const tableau = [];

//  Céer un tableau avec tous les mois de l'année
// Faire un console.log du tableau
// Afficher en console janvier et décembre
// Afficher tous les mois de l'année avec une boucle
// Transformer cette boucle en fonction sans paramètre

const monthOfYear = ["javier", "février","mars", "avril", "décembre"];
console.log(monthOfYear);

console.log(monthOfYear[0]);
console.log(monthOfYear[2]);
console.log(monthOfYear.length);

// let (i = 0; i < monthOfYear.length; i++) { 
//     console.log(monthOfYear[i])
// }

